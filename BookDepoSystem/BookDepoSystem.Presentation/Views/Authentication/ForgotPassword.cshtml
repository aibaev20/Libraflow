@model ForgotPasswordViewModel

@{
    Layout = "_AuthLayout";
}

<div class="text-center">
    <h1 class="h4 text-gray-900 mb-4">@T.ForgotPasswordTitle</h1>
</div>
<form class="user" asp-action="ForgotPassword" method="post">
    <div class="form-group">
        <input asp-for="Email"
               class="form-control"
               aria-describedby="emailHelp"
               placeholder="@T.EmailPlaceholderText">
        <span class="text-danger text-small" asp-validation-for="Email"></span>
    </div>
    <button type="submit" class="btn btn-primary btn-user btn-block">
        @T.SendResetRequestButtonText
    </button>
</form>
<hr>
<div class="text-center">
    <a class="small" href="@Url.Action("Login")">@T.IRememberedMyPasswordLinkText</a>
</div>